version: "3.8"
services:
  db:
    image: postgres:alpine
    restart: always
    environment:
      POSTGRES_USER: yo
      POSTGRES_PASSWORD: una_clave_muy_segura_123
      POSTGRES_DB: tienda
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
